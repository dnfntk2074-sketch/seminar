name: Deploy Gangbuk TFA Toolbox Pages
on:
  push:
    branches: [main]
    paths:
      - 'gangbuk-toolbox/**'
      - '.github/workflows/deploy-gangbuk-toolbox-pages.yml'
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: gangbuk-toolbox-pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: gangbuk-toolbox
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4
